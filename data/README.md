# Datos locales

Los datasets descargados para pruebas o investigación viven en `data/local/` y están ignorados por Git. No forman parte del runtime ni deben contener datos clínicos reales sin anonimizar.
